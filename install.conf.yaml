- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty: kitty
    ~/.config/nvim: nvim
    ~/.zshrc: zsh/zshrc
    ~/.zprofile: zsh/zprofile
    ~/Library/Rime: Rime


- shell:
  - [git submodule update --init --recursive, Installing submodules]

