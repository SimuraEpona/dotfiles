- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/nvim: nvim
    ~/.zshrc: zsh/zshrc
    ~/.zprofile: zsh/zprofile


- shell:
  - [git submodule update --init --recursive, Installing submodules]

